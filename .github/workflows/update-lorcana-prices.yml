name: Update Lorcana prices

on:
  workflow_dispatch:
  schedule:
    - cron: "17 4 * * *"

permissions:
  contents: write

jobs:
  update-prices:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Fetch prices
        env:
          RAPIDAPI_KEY: ${{ secrets.RAPIDAPI_KEY }}
          CARDMARKET_PRICE_GUIDE_URL: ${{ vars.CARDMARKET_PRICE_GUIDE_URL }}
          CARDMARKET_LORCANA_NONSINGLES_URL: ${{ vars.CARDMARKET_LORCANA_NONSINGLES_URL }}
          CARDMARKET_ACCESSORIES_URL: ${{ vars.CARDMARKET_ACCESSORIES_URL }}
          CARDMARKET_ACCESSORIES_PRICE_GUIDE_URL: ${{ vars.CARDMARKET_ACCESSORIES_PRICE_GUIDE_URL }}
        run: python tools/fetch_lorcana_prices.py

      - name: Commit updated prices
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: Update Lorcana prices
          file_pattern: data/lorcana-prices-v1.json
